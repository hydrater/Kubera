using UnityEngine;
using System.Collections;

public class RandomGenarator : MonoBehaviour {

	// Use this for initialization
	void Start () {
		int n = Random.Range(0,10);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
